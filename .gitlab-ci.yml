image: ubuntu

stages:
- update
- make

update:
    stage: update
    script:
        - apt update
        - apt upgrade -y
        - apt install -y g++ make libgtest-dev astyle
        
make:
    stage: make
    script: make