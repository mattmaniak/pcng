image: debian

stages:
- update
- analyze
- test
- build

update:
    stage: update
    script:
        - apt update
        - apt upgrade -y
        - apt install -y g++ make libgtest-dev astyle

test:
    stage: test
    script: make test

build:
    stage: build
    script: make